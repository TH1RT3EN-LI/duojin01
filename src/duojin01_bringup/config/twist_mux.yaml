twist_mux:
  ros__parameters:

    topics:
      navigation:
        topic: /cmd_vel
        timeout: 0.5
        priority: 80

      keyboard:
        topic: /cmd_vel_keyboard
        timeout: 0.1
        priority: 90

      estop:
        topic: /cmd_vel_estop
        timeout: 0.1
        priority: 100

      teleop_normal:
        topic: /cmd_vel_normal
        timeout: 0.01
        priority: 60

      teleop_slow:
        topic: /cmd_vel_slow
        timeout: 0.01
        priority: 50
